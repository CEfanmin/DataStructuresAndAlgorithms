#include <iostream>
#include "inheritance.h"

/*int main(){
    Rectangle rect;

    rect.setWidth(5);
    rect.setHeight(7);

    rect.setLength(6);
    std::cout << "Total volume: " << rect.getVolume(rect.getArea()) << std::endl;
    std::cout << "Total area: " << rect.getArea() << std::endl;

    return 0;
}*/
